requests>=2.31
python-dotenv>=1.0
Pillow>=10.0
PyJWT>=2.8
# Veo 3.1 + Gemini image editing (veo_carve.py, make_ladder.py --engine gemini).
# >= 2.x required: older SDKs lack GenerateVideosConfig.last_frame.
google-genai>=2.0
